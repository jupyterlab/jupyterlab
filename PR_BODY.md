## Description

Fixed a duplicated sentence in the `showJumpToRecentExecutionButton` setting description in `packages/notebook-extension/schema/tracker.json`.

The sentence "scrolls to the currently executing cell or, if none, the most recently executed cell." was accidentally duplicated.

## Type of change

- [x] Bug fix (duplicate text removed)