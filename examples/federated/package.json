{
  "name": "@jupyterlab/example-federated",
  "version": "3.0.0-alpha.21",
  "private": true,
  "scripts": {
    "build": "$npm_execpath run build:core && $npm_execpath run build:middle && $npm_execpath run build:md && $npm_execpath run build:phosphor",
    "build:core": "cd core_package && $npm_execpath run build",
    "build:md": "cd md_package && $npm_execpath run build",
    "build:middle": "cd middle_package && $npm_execpath run build",
    "install:all": "$npm_execpath run install:core && $npm_execpath run install:middle && $npm_execpath run install:md && $npm_execpath run install:phosphor",
    "install:core": "cd core_package && $npm_execpath install",
    "install:md": "cd md_package && $npm_execpath install",
    "install:middle": "cd middle_package && $npm_execpath install"
  }
}
